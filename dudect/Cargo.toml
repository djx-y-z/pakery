[package]
name = "pakery-dudect"
version = "0.0.0"
publish = false
edition = "2021"
# Matches the workspace MSRV (1.85). This crate stays a standalone workspace
# for its dependency tree (clap 2 era), not for its MSRV; the dudect.yml
# workflow runs it on current stable.
rust-version = "1.85"

[dependencies]
dudect-bencher = "0.7"
rand_core = { version = "0.9", features = ["os_rng"] }

pakery-core = { path = "../pakery-core" }
pakery-spake2 = { path = "../pakery-spake2" }
pakery-crypto = { path = "../pakery-crypto", features = ["ristretto255", "spake2"] }

# This is a standalone workspace, deliberately excluded from the root one so
# the MSRV / no_std / minimal-versions / wasm / audit CI jobs never see
# dudect-bencher's dependency tree (clap 2 era).
[workspace]

[[bin]]
name = "dudect_ct"
path = "src/main.rs"
test = false
doc = false
bench = false
